const mongoose = require("mongoose");
const bcrypt = require("bcryptjs");
const jwt = require("jsonwebtoken");

const googleAuthSchema = new mongoose.Schema({

})

module.exports = mongoose.model(" ", googleAuthSchema);