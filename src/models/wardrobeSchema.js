const mongoose = require("mongoose");
const bcrypt = require("bcryptjs");
const jwt = require("jsonwebtoken");

const wardrobeSchema = new mongoose.Schema({

})

module.exports = mongoose.model(" ", wardrobeSchema);